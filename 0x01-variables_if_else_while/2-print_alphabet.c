#include <stdio.h>
#include <stdio.h>

/**
 * main - prints the alphabet using ASCII
 *
 * Return: zero
 */

int main(void)
{
	char ch;

	for (ch = 97; ch < 123; ch++)
	{
		putchar(ch);
	}
	return (0);
}
