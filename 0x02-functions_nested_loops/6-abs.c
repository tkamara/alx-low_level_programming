#include "main.h"

/**
 * _abs - function to print absolute value of integer
 *
 * @int: integer to be checked
 *
 * Return: integer
 */

int _abs(int)
{
	int a;

	if (a < 0)
	{
		int b;

		b = a * -1;
		return (b);
	}
	else
	{
		return (a);
	}
}
