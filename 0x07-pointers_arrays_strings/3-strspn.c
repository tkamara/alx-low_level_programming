#include "main.h"

/**
 * _strspn - gets length of prefix substring
 * @s: the string
 * @accept: bytes from source
 *
 * Return: number of bytes in s
 */

unsigned int _strspn(char *s, char *accept)
{
}
